=== RUST FIBONACCI CODE ===
// Generated by MiniPar ms-codegen-rust

#[derive(Debug, Default)]
struct Main {
    _tag: i32,
}
impl Main {
    fn new() -> Main {
        Main::default()
    }
}

fn Main_fib(this: &mut Main, n: f64) -> f64 {
    let mut t0: f64 = if (n as f64) <= (1 as f64) { 1.0 } else { 0.0 };
    if (t0 as f64) == 0.0 { break '_L0; }
    return n as f64;
    '_L0: loop {
    let mut t1: f64 = (n as f64) - (1 as f64);
    let mut t2: f64 = Main_fib(&mut this, t1);
    let mut t3: f64 = (n as f64) - (2 as f64);
    let mut t4: f64 = Main_fib(&mut this, t3);
    let mut t5: f64 = (t2 as f64) + (t4 as f64);
    return t5 as f64;
}

fn Main_run(this: &mut Main) {
    let mut t6: f64 = Main_fib(&mut this, 7);
    let mut r: f64 = (t6) as f64;
    println!("{}", r as f64);
}

fn main() {
    let mut _main = Main::new();
    Main_run(&mut _main);
}