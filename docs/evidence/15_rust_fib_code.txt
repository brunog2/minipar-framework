// Generated by MiniPar RustASTEmitter

#[derive(Debug, Default, Clone)]
pub struct Main {
    _tag: i32,
}
impl Main {
    pub fn new() -> Self { Self::default() }
}

fn Main_fib(this: &mut Main, n: f64) -> f64 {
    if ((if (n as f64) <= (1.0_f64 as f64) { 1.0_f64 } else { 0.0_f64 })) != 0.0 {
        return n as f64;
    }
    return ((Main_fib(this, ((n as f64) - (1.0_f64 as f64))) as f64) + (Main_fib(this, ((n as f64) - (2.0_f64 as f64))) as f64)) as f64;
}

fn Main_run(this: &mut Main) {
    println!("{}", Main_fib(this, 7.0_f64));
}

fn main() {
    let mut _main = Main::new();
    Main_run(&mut _main);
}
