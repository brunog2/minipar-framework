[project]
name = "minipar-core"
version = "0.1.0"
description = "Lexer, parser e análise semântica MiniPar 2026.1 (OO)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["minipar_core"]
